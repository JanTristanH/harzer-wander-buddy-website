---
layout: null
sitemap: false
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.sitemaps.org/schemas/sitemap/0.9 http://www.sitemaps.org/schemas/sitemap/0.9/sitemap.xsd"
        xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  {% assign collections = site.collections | where_exp: "collection", "collection.output != false" %}
  {% for collection in collections %}
    {% assign documents = collection.docs | where_exp: "document", "document.sitemap != false" %}
    {% for document in documents %}
  <url>
    <loc>{{ document.url | replace: "/index.html", "/" | absolute_url | xml_escape }}</loc>
      {% if document.last_modified_at or document.date %}
    <lastmod>{{ document.last_modified_at | default: document.date | date_to_xmlschema }}</lastmod>
      {% endif %}
  </url>
    {% endfor %}
  {% endfor %}

  {% assign pages = site.html_pages | where_exp: "html_page", "html_page.sitemap != false" | where_exp: "html_page", "html_page.url != '/404.html'" %}
  {% for html_page in pages %}
    {% assign url_prefix = html_page.url | slice: 0, 5 %}
    {% unless url_prefix == "/page" or html_page.url contains "/category/" %}
  <url>
    <loc>{{ html_page.url | replace: "/index.html", "/" | absolute_url | xml_escape }}</loc>
      {% if html_page.last_modified_at %}
    <lastmod>{{ html_page.last_modified_at | date_to_xmlschema }}</lastmod>
      {% endif %}
  </url>
    {% endunless %}
  {% endfor %}
</urlset>
